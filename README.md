RxActivity
==========
Work in progress.

```java
RxActivity.startForResult(activity, new Intent(this, ExampleActivity.class))
    .subscribe(data -> {
    
    }, throwable -> {
    
    });
```

Usage
-----